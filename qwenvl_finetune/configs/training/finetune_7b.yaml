# Qwen2.5-VL 7B Model Fine-tuning Configuration

# Model Configuration
model:
  model_size: "7B"
  model_name: "Qwen/Qwen2.5-VL-7B-Instruct"
  torch_dtype: "bfloat16"
  attn_implementation: "flash_attention_2"
  trust_remote_code: true

# Training Configuration
training:
  # Basic training parameters
  output_dir: "./checkpoints/qwen25vl_7b_finetune"
  num_train_epochs: 3
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 2
  gradient_accumulation_steps: 16
  eval_accumulation_steps: 1
  
  # Learning rate and optimization
  learning_rate: 1.5e-5
  weight_decay: 0.01
  adam_beta1: 0.9
  adam_beta2: 0.999
  adam_epsilon: 1.0e-8
  max_grad_norm: 1.0
  
  # Learning rate scheduler
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.03
  warmup_steps: 0
  
  # Mixed precision
  bf16: true
  fp16: false
  tf32: true
  
  # Memory optimization
  gradient_checkpointing: true
  dataloader_pin_memory: true
  dataloader_num_workers: 2
  
  # Evaluation and saving
  evaluation_strategy: "steps"
  eval_steps: 500
  save_strategy: "steps"
  save_steps: 500
  save_total_limit: 2
  load_best_model_at_end: true
  metric_for_best_model: "eval_loss"
  greater_is_better: false
  
  # Logging
  logging_dir: "./logs/qwen25vl_7b_finetune"
  logging_strategy: "steps"
  logging_steps: 50
  report_to: ["tensorboard", "wandb"]
  
  # Other settings
  seed: 42
  remove_unused_columns: false
  label_names: ["labels"]

# DeepSpeed Configuration
deepspeed:
  enabled: true
  config_file: "./configs/deepspeed/zero3_7b.json"
  
# Data Configuration
data:
  max_length: 2048
  max_image_tokens: 4096
  image_aspect_ratio: "anyres"
  conversation_template: "chatml"
  system_message: "You are a helpful AI assistant."
  
  # Data validation
  validate_images: true
  validate_conversations: true
  skip_invalid_samples: true
  
  # Data augmentation
  enable_image_augmentation: false
  augmentation_prob: 0.2

# Hardware Configuration
hardware:
  # Memory settings
  max_memory_gb: null  # Auto-detect
  use_cpu_offload: true
  
  # Multi-GPU settings
  dataloader_drop_last: true
  ddp_find_unused_parameters: false

# Logging and Monitoring
monitoring:
  log_memory_usage: true
  memory_logging_interval: 100
  
  # WandB settings
  wandb_project: "qwen25vl-finetune"
  wandb_name: "qwen25vl-7b-finetune"
  wandb_tags: ["qwen25vl", "7B", "finetune", "multimodal"]

# Checkpoint Management
checkpointing:
  save_safetensors: true
  checkpoint_keep_steps: 3
  resume_from_checkpoint: null